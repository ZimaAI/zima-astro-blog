# Managed by zima-astro-blog.
server {
    listen 80;
    listen [::]:80;
    server_name aboutme.zimagent.top;
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/zima-astro-blog/acme;
        try_files $uri =404;
    }
    include /etc/nginx/snippets/zima-astro-blog.conf;
}
